import React from "react";

function LocationList({ locations, inputLoc, onLocationSelect }) {
  return (
    <div className={`locales_00 ${inputLoc ? '' : 'hola'}`}>
      {/* Location list components */}
    </div>
  );
}

export default LocationList;
