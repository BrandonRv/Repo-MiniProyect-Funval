import React from "react";

function SearchBar({ inputLoc, onLocationChange, onSearch, isOpenMenu, setIsOpenMenu }) {
  return (
    <div className={`menu_fondo_b ${isOpenMenu ? 'open' : ''}`}>
      {/* Search bar components */}
    </div>
  );
}

export default SearchBar;
